%{
#include "y.tab.h"
#include <stdlib.h>
#include <string.h>
%}

%%

[0-9]+\.[0-9]+([eE][+-]?[0-9]+)? {
    yylval.dval = atof(yytext);
    return DOUBLE;
}

[0-9]+ {
    yylval.dval = atof(yytext);
    return INTEGER;
}

[a-zA-Z_][a-zA-Z0-9_]* {
    yylval.sval = strdup(yytext);
    return IDENTIFIER;
}

[ \t\r]+        ;

\n              return '\n';
";"             return ';';

"+"             return '+';
"-"             return '-';
"*"             return '*';
"/"             return '/';
"^"             return '^';
"("             return '(';
")"             return ')';
"="             return '=';

.               return yytext[0];

%%

int yywrap()
{
    return 1;
}
